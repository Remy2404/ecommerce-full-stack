/**
 * Admin Dashboard Widgets
 * Reusable stat widgets with auto-refresh and loading states
 */

export { StatWidget, StatWidgetSkeleton, type StatWidgetProps } from './stat-widget';
export { DashboardWidgets } from './dashboard-widgets';
